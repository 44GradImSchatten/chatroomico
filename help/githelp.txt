cd .. zurück
cd <ordner> : na vigieren
ls aktuelles verzeichn is
explorer . : öffnet explorer

git init : neu
git clone <adresse> : von dive
git push
git status : änderungen zeigt er
git add / -a : fügt hinzu
git commit : speichertxx
git push origin master
git checkout -b 'marco': neuer  branch - wechselt zu ihm
git checkout 'marco' : wechselt
git pull : vom server die änderungen zusammentun

c at: datein ausgeben
vim ... : date bearbeiten
esc --> :w :q

git config --global --unset https.proxy
git config --global --unset http.proxy
git config --global http.proxy web-proxy:8088
git config --global https.proxy web-proxy:8088

git reset --hard head
//verwirft alle änderungen seit dem commit

ipconfig /renew
ipconfig -renew